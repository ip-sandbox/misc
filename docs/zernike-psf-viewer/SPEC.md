# Zernike PSF Viewer 仕様書

| 項目 | 内容 |
| --- | --- |
| 文書名 | Zernike 係数入力による瞳面・PSF 可視化ツール 仕様書 |
| 版 | 0.1 (ドラフト) |
| 作成日 | 2026-08-30 |
| 対象読者 | 本ツールの実装者・レビュア |
| ステータス | 技術選定を含む初版。実装着手前のレビュー対象 |

---

## 1. 背景と目的

### 1.1 背景

光学系の波面収差は Zernike 多項式の係数列で表現されることが多い。設計・評価の現場では
「この係数を少し変えたら瞳面（波面）と PSF がどう変わるか」を対話的に確かめたい場面が
頻繁にあるが、既存手段は次の理由で使いにくい。

- 商用光学設計ソフトはライセンスが必要で、手元の Windows PC で気軽に開けない。
- Python (numpy/matplotlib, poppy, prysm 等) は簡単だが、**本件の対象環境では Python を
  利用できない前提**である。
- Excel では 2 次元 FFT と画像表示が現実的でない。

### 1.2 目的

Zernike 係数を GUI から入力すると、**瞳面（振幅・位相）と PSF がリアルタイムに表示される**
スタンドアロンツールを、**Python に依存せず Windows 上で単体動作**する形で提供する。

### 1.3 想定ユースケース

| ID | ユースケース |
| --- | --- |
| UC-1 | 特定の Zernike 項（例: 球面収差 0.1λ RMS）を単独で入れ、PSF の崩れ方を確認する |
| UC-2 | 干渉計や設計ソフトから出力された係数列 (CSV) を読み込み、PSF と Strehl 比を確認する |
| UC-3 | 中心遮蔽（副鏡）や矩形瞳など、瞳形状を変えたときの PSF 変化を比較する |
| UC-4 | スライダで係数を動かし、収差の見え方を教育・説明用にデモする |
| UC-5 | 表示中の波面 / PSF を PNG・CSV で書き出し、報告書に貼る |

### 1.4 用語

| 用語 | 意味 |
| --- | --- |
| 瞳面 (pupil plane) | 射出瞳における複素振幅分布。本書では `P(x,y) = A(x,y)·exp(i·2π·W(x,y)/λ)` |
| 波面収差 `W` | 参照球面からの光路差 (OPD)。単位は波長 λ または nm |
| PSF | 点像分布関数。瞳関数のフーリエ変換の強度 |
| λ/D | 回折限界の角度スケール。焦点面上では `λ·F#` |
| `q` | PSF のサンプリング密度 = 1 λ/D あたりの画素数 |
| Strehl 比 | 無収差 PSF ピークに対する収差 PSF ピークの比 |
| RMS / PV | 瞳内での波面の二乗平均平方根 / 最大値−最小値 |

---

## 2. スコープ

### 2.1 本ツールが行うこと (In scope)

- Zernike 係数の GUI 入力（表・スライダ・直接入力・CSV/JSON 読み書き）
- 瞳面の波面マップ（位相）・振幅マップの表示
- 単色・非コヒーレント・遠方場（フラウンホーファー）近似での PSF 計算と表示
- 評価指標（RMS / PV / Strehl / FWHM / エンサークルドエネルギー）の算出
- MTF（1 次元断面および 2 次元）の表示（オプション機能）
- スルーフォーカス（デフォーカスを振った連続表示）（オプション機能）
- PNG / CSV / JSON へのエクスポート

### 2.2 本ツールが行わないこと (Out of scope)

- 光線追跡・レンズ設計・最適化
- 多波長合成（ポリクロマティック PSF）※ v2 以降の候補
- 視野内の収差分布（フィールド依存）や像シミュレーション（畳み込み）
- 偏光・スカラー近似を超える厳密回折計算
- 近軸外・大 NA でのベクトル回折（高 NA 補正）
- 3D 表示（波面の立体レンダリング）※ v2 以降の候補

---

## 3. 技術選定

### 3.1 制約条件

| ID | 制約 |
| --- | --- |
| C-1 | Windows 10 (21H2 以降) / Windows 11、x64 で動作すること |
| C-2 | Python ランタイムに依存しないこと |
| C-3 | 管理者権限なしで実行できること（インストーラ不要が望ましい） |
| C-4 | 2D FFT (N=512〜1024) をインタラクティブ速度で回せること |
| C-5 | 2 次元画像をカラーマップ付きで高速描画できること |

### 3.2 候補比較

| 案 | 技術 | 配布 | 数値性能 | GUI 開発コスト | 描画 | 主なリスク |
| --- | --- | --- | --- | --- | --- | --- |
| **A** | **C# / .NET 8 + WinForms** | 自己完結 single-file `.exe`（〜70 MB）または framework-dependent（〜1 MB） | ◎ JIT ネイティブ相当。1024² FFT で数十 ms | ◎ 低い。表・スライダ・画像が標準部品 | ○ `Bitmap` + `LockBits` で十分高速 | 単一 exe のサイズ、未署名 exe の AV 誤検知 |
| B | C# / .NET 8 + WPF or Avalonia | 同上 | ◎ | △ XAML の学習コスト | ◎ GPU 合成 | オーバースペック |
| C | C++ + Dear ImGui (DX11) | 単一 `.exe`（数 MB） | ◎ 最速 | △ ビルド環境整備が重い | ◎ GPU テクスチャ | ビルド・保守コスト |
| D | Rust + egui/eframe | 単一 `.exe`（〜10 MB） | ◎ | ○ | ◎ | Rust 開発者が必要 |
| **E** | **PowerShell 5.1 + WinForms（数値部は `Add-Type` で C# を内包）** | `.ps1` 1 ファイル。ビルド不要、OS 標準機能のみ | ○ `Add-Type` した C# 部分はネイティブ相当（PowerShell で直接ループを回すと 100 倍以上遅く実用外） | ○ | △ `Bitmap` 経由 | 実行ポリシー、単一ファイルが巨大化しやすい |
| F | Electron + TypeScript (WASM/WebGL) | 配布物 150 MB 超 | ○ WASM 化が前提 | ○ Web 技術者なら容易 | ◎ WebGL | 配布サイズ・起動時間・依存管理 |
| **G** | **TUI（C# + Spectre.Console / Rust + ratatui）** | 単一 `.exe` | ◎ | ○ | △ 半角ブロック文字 + 24bit カラーで縦 2 画素/セル。Sixel は Windows Terminal 1.22+ のみ | 端末依存、細部が見えない |

### 3.3 推奨構成

**主案: A（C# / .NET 8 + WinForms）を採用する。**

理由:

1. **配布が最も素直** — `dotnet publish -r win-x64 --self-contained -p:PublishSingleFile=true`
   で 1 個の `.exe` になる。インストーラも管理者権限も不要（制約 C-1〜C-3 を満たす）。
2. **数値計算が速い** — FFT を自前実装しても 1024² を数十 ms で処理でき、
   スライダ操作へのリアルタイム追従（制約 C-4）が可能。
3. **GUI 部品が枯れている** — `DataGridView`（係数表）、`TrackBar`（スライダ）、
   `PictureBox`（画像）で必要な UI がほぼ標準部品で組める。学習コストが低い。
4. **他形態への展開が容易** — 計算部を `ZernikePsf.Core.dll` として切り出せば、
   案 G の TUI、案 E の PowerShell スクリプト（`Add-Type -Path` で DLL 読込）が
   同一コアを再利用して実装できる。

**副案として次を併記する。**

- **案 E（PowerShell）**: 「対象 PC に .NET SDK もビルド成果物も置けない」状況の逃げ道。
  `.ps1` 1 ファイルをコピーするだけで動く。数値カーネルは `Add-Type` で C# ソースを
  文字列として埋め込み、初回起動時にコンパイルする。**PowerShell の言語機能だけで
  2D FFT を回すのは性能的に不可能**なので、この構成は必須である。
- **案 G（TUI）**: SSH / リモート運用や、GUI が使えない環境向けの追加フロントエンド。
  コアを共有するため追加コストは小さい。v2 のオプションとする。

**案 F（Electron）は採用しない。** 配布サイズと依存管理のコストが、本ツールの規模
（単機能のビューア）に見合わない。

### 3.4 決定が覆る条件

| 条件 | 切替先 |
| --- | --- |
| 対象 PC に .NET 8 ランタイムも配布物も置けない | 案 E |
| Web ブラウザ配布（社内ポータル等）が要件に追加された | 案 F または WASM 単体 |
| 3D 表示・大規模データのリアルタイム描画が要件に追加された | 案 B / C / D |
| CUI・リモート環境が主戦場になった | 案 G |

---

## 4. アーキテクチャ

```
ZernikePsf.sln
├─ ZernikePsf.Core/            … GUI 非依存。netstandard2.0 でビルドし PS からも利用可
│   ├─ ZernikeIndex.cs         … OSA/ANSI ⇔ Noll ⇔ Fringe ⇔ (n,m) の相互変換
│   ├─ ZernikePolynomial.cs    … R_n^m と正規化係数、基底マップ生成
│   ├─ PupilBuilder.cs         … 瞳マスク（円形・中心遮蔽・スパイダー・矩形・任意画像）
│   ├─ Fft.cs                  … 1D/2D 複素 FFT（radix-2 Cooley–Tukey, in-place）
│   ├─ PsfCalculator.cs        … 瞳関数 → PSF / OTF / MTF
│   ├─ Metrics.cs              … RMS, PV, Strehl, FWHM, EE
│   └─ Model/                  … OpticalSetup, ZernikeSet, Result（POCO, JSON 直列化対象）
├─ ZernikePsf.Rendering/       … 2D 配列 → Bitmap（カラーマップ・スケーリング）
├─ ZernikePsf.WinForms/        … GUI（主案 A）
├─ ZernikePsf.Tui/             … TUI（案 G, v2）
├─ ZernikePsf.Cli/             … バッチ実行・自動テスト用（係数ファイル → PNG/CSV）
└─ ZernikePsf.Tests/           … 単体テスト（xUnit）
```

### 4.1 計算パイプライン

```
 [Zernike 係数 c_j]         [光学設定 λ, F#, ε, N, D]
        │                            │
        ▼                            ▼
  基底マップキャッシュ  ────►  波面 W(x,y) = Σ c_j Z_j(x,y)
   （項ごとに 1 回だけ生成）           │
                                      ▼
                     瞳関数 P = A(x,y)·exp(i·2π·W/λ)   ← 瞳マスク A
                                      │
                              zero-pad → N×N
                                      ▼
                                  2D FFT + fftshift
                                      │
                     ┌────────────────┼───────────────┐
                     ▼                ▼               ▼
                 PSF = |F|²      指標 (Strehl 等)   OTF/MTF
                     │
                     ▼
              スケーリング(log/γ) → カラーマップ → Bitmap
```

**性能上の要点:** 基底マップ `Z_j(x,y)`（`N_pupil × N_pupil` の `double[]`）は
係数に依存しないので、瞳設定が変わらない限り再利用する。係数変更時の再計算は
`W = Σ c_j Z_j` の線形結合（O(N²·K)）と FFT のみとなり、スライダのリアルタイム追従が可能。

---

## 5. 計算仕様

### 5.1 Zernike 多項式

単位円 `0 ≤ ρ ≤ 1`, `0 ≤ θ < 2π` 上で定義する。

```
Z_n^m(ρ,θ) = N_n^m · R_n^|m|(ρ) · cos(mθ)      (m ≥ 0)
Z_n^m(ρ,θ) = N_n^m · R_n^|m|(ρ) · sin(|m|θ)    (m < 0)
```

動径多項式:

```
                (n-|m|)/2       (-1)^s (n-s)!
R_n^|m|(ρ) =      Σ      ──────────────────────────────── ρ^(n-2s)
                 s=0     s! ((n+|m|)/2 - s)! ((n-|m|)/2 - s)!
```

`n - |m|` が奇数のとき `R_n^|m| ≡ 0`。

正規化係数（**RMS 正規化 / OSA・ANSI 標準**）:

```
N_n^m = sqrt( 2(n+1) / (1 + δ_m0) )      δ_m0 は m=0 のとき 1、それ以外 0
```

この正規化では単位円上で `(1/π)∫∫ Z_i Z_j ρ dρ dθ = δ_ij` が成り立ち、
係数 `c_j` はそのまま「その項の RMS 波面誤差」を表す。

**設定で正規化を OFF（`N_n^m = 1`）にできること。** Fringe 系のデータは
非正規化（縁での値 = 係数）で与えられることが多いため。

### 5.2 インデックス方式

3 方式をサポートし、UI から切り替えられる。

| 方式 | 開始 | 変換式 |
| --- | --- | --- |
| OSA / ANSI | j = 0 | `j = (n(n+2) + m) / 2` |
| Noll | j = 1 | n ごとに `\|m\|` 昇順。同一 `\|m\|` では **j が偶数 → cos (m>0)、奇数 → sin (m<0)** |
| Fringe (University of Arizona) | j = 1 | `j = (1 + (n+\|m\|)/2)² − 2\|m\| + ⌊(1 − sgn m)/2⌋` |

対応表は付録 A を参照。**内部表現は常に `(n, m)`** とし、入出力時のみ方式変換する。

### 5.3 波面

```
W(ρ,θ) = Σ_j c_j · Z_j(ρ,θ)        [単位: 波長 λ]
```

係数の入力単位は **λ（波数）** と **nm** を切り替え可能とし、内部は λ 単位で保持する
（nm 入力時は `c[λ] = c[nm] / λ[nm]`）。

ピストン（n=0）とチルト（n=1）は PSF の形状を変えず位置だけを動かすため、
**「ピストン/チルトを除外して指標を計算する」チェックボックス**を設ける（既定 ON）。

### 5.4 瞳関数

```
P(x,y) = A(x,y) · exp( i · 2π · W(x,y) )      (W は λ 単位)
```

`A(x,y)` は瞳の振幅透過率で、次を合成して構成する。

| 要素 | パラメータ |
| --- | --- |
| 円形開口 | 直径 = `D_pix` サンプル |
| 中心遮蔽 | 遮蔽比 `ε = 0.0〜0.9`（`ρ < ε` を 0 にする） |
| スパイダー | 本数 (0/3/4)、幅 [サンプル]、回転角 |
| 矩形／正方 | 幅・高さ [サンプル] |
| ガウシアンアポダイゼーション | `A = exp(−(ρ/w)²)`、`w` 可変 |
| 任意マスク | 8bit グレースケール PNG を読み込み `A = pixel/255` |

> **注意（実装者向け）**: 中心遮蔽や非円形瞳では Zernike の直交性が崩れる。
> したがって **RMS は係数の二乗和 `sqrt(Σ c_j²)` から求めてはならず、
> マスク内の波面マップから数値的に計算する**こと。UI 上でもこの旨を注記する。

### 5.5 PSF

`N × N` の複素配列に瞳関数をゼロパディングして配置し、2D FFT を掛ける。

```
PSF(u,v) = | FFT2( P ) |²        （fftshift して中心を原点に）
```

**サンプリング関係（本ツールの最重要パラメータ）**

- `D_pix` : 瞳直径にあたるサンプル数（既定 256）
- `N` : FFT 配列サイズ、2 のべき乗（既定 1024）
- `q = N / D_pix` : **1 λ/D あたりの PSF 画素数**

このとき焦点面での画素スケールは

```
Δθ = (1/q) · (λ/D)        [rad]
Δx' = (1/q) · λ · F#       [mm など長さ単位]
```

| 条件 | 値 | 備考 |
| --- | --- | --- |
| ナイキスト条件 | `q ≥ 2` | これを下回る設定は UI で警告 |
| 推奨値 | `q = 4` | ピーク位置・FWHM が安定して求まる |
| 既定 | `D_pix = 256, N = 1024` → `q = 4` | |

UI では `N` と `D_pix` を直接入力させるのではなく、
**「瞳サンプル数」と「PSF サンプリング q」を入力し、`N = q · D_pix` を 2 のべき乗に
切り上げて自動決定する**方式とする（`N` の実値と実効 `q` は表示する）。

PSF の正規化は次の 2 方式を切り替え可能とする。

1. **総和正規化**: `Σ PSF = 1`（エンサークルドエネルギー計算用）
2. **無収差ピーク正規化**: 同一瞳マスクの無収差 PSF のピークを 1 とする
   （表示ピーク値がそのまま Strehl 比になる）— 既定

### 5.6 評価指標

| 指標 | 定義 | 備考 |
| --- | --- | --- |
| PV | `max(W) − min(W)`（マスク内） | λ 単位 |
| RMS `σ` | `sqrt( Σ A²(W−W̄)² / Σ A² )` | マスク内。振幅重み付き |
| Strehl（定義値） | `S = abs( Σ A·e^{i2πW} )² / (Σ A)²` | 原点における強度比。厳密 |
| Strehl（実測ピーク比） | `max(PSF_ab) / max(PSF_ideal)` | チルト起因のずれを含まない値。両方表示 |
| Maréchal 近似 | `S ≈ exp(−(2πσ)²)` | `σ` は λ 単位 RMS。`σ ≲ 0.1λ` で有効 |
| FWHM | PSF の半値全幅 | `λ/D` 単位。放射平均から補間で算出 |
| EE(r) | 半径 `r` 内の全エネルギー比 | 半径は `λ/D` 単位 |

### 5.7 OTF / MTF（オプション機能）

```
OTF(f) = FFT2( PSF ) / FFT2( PSF )|_{f=0}
MTF(f) = |OTF(f)|
```

周波数軸は `Δf = q / (N · λ · F#)`。理想瞳のカットオフ `f_c = 1/(λ·F#)` は
配列上でちょうど `D_pix` 画素目にあたる（実装の検算に使える）。

### 5.8 数値仕様

| 項目 | 仕様 |
| --- | --- |
| 実数型 | `double`（倍精度） |
| FFT | radix-2 Cooley–Tukey、in-place、twiddle 係数を事前計算してキャッシュ |
| FFT サイズ | 256 / 512 / 1024 / 2048（2 のべき乗のみ） |
| 角度計算 | `Math.Atan2(y, x)`。`ρ = 0` の点では `θ = 0` |
| 座標 | 画素中心基準。`N` が偶数のとき原点は `N/2` 画素目（fftshift 後） |
| 動径多項式 | 階乗はガンマ関数ではなく整数テーブルで計算（`n ≤ 20` を保証範囲とする） |
| 最大次数 | `n ≤ 10`（OSA j ≤ 65）を必須、`n ≤ 20` を努力目標 |

---

## 6. 機能要件

優先度: **M** = 必須 (v1), **S** = 望ましい (v1), **C** = v2 以降

| ID | 機能 | 優先 | 内容 |
| --- | --- | --- | --- |
| FR-01 | 係数表入力 | M | 各項について「インデックス / (n,m) / 名称 / 係数値 / スライダ」を表形式で表示。数値直接入力とスライダの双方向同期 |
| FR-02 | 表示項数の変更 | M | 最大次数 `n_max` または項数 `K` を指定して表の行数を変更（既定: n ≤ 4 = 15 項） |
| FR-03 | インデックス方式切替 | M | OSA/ANSI・Noll・Fringe を切替。切替時は `(n,m)` を保持したまま行の並びと表示番号のみ変更する（値は失わない） |
| FR-04 | 正規化切替 | M | RMS 正規化 ON/OFF。切替時に「係数値を保持するか、波面が変わらないよう換算するか」を確認するダイアログを出す |
| FR-05 | 単位切替 | M | 係数の単位を λ / nm で切替。波長 λ [nm] を入力（既定 550 nm） |
| FR-06 | 一括操作 | M | 全係数ゼロクリア、ランダム生成（RMS 総量を指定）、単一項のみ残す |
| FR-07 | 瞳マスク設定 | M | 5.4 節の各パラメータを UI から設定 |
| FR-08 | 波面マップ表示 | M | マスク外は背景色。発散カラーマップ（0 を中心色）。カラーバー付き |
| FR-09 | 瞳振幅マップ表示 | S | グレースケール。マスク・アポダイゼーションの確認用 |
| FR-10 | PSF 表示 | M | リニア / 対数（下限 dB 指定） / γ 補正 を切替。カラーマップ選択 |
| FR-11 | PSF ズーム | M | 表示範囲を `λ/D` 単位で指定（既定 ±16 λ/D）。マウスホイールでも変更 |
| FR-12 | 断面プロット | S | 波面・PSF の水平/垂直断面を折れ線グラフ表示。PSF は理想 Airy と重畳表示 |
| FR-13 | 指標パネル | M | 5.6 節の指標を数値表示。ピストン/チルト除外のチェックボックス付き |
| FR-14 | リアルタイム更新 | M | スライダのドラッグ中も追従（8.2 節の性能要件を満たすこと） |
| FR-15 | プリセット | S | 「単一収差サンプル」「代表的な望遠鏡瞳」などの組込プリセットを選択して読み込む |
| FR-16 | 保存 / 読込 | M | 全設定を JSON で保存・読込（7.1 節） |
| FR-17 | CSV 入出力 | M | 係数列のみを CSV で入出力（7.2 節） |
| FR-18 | 画像エクスポート | M | 波面・PSF・断面を PNG で保存。カラーバーとパラメータ注記を焼き込むオプション |
| FR-19 | 数値エクスポート | S | 波面マップ・PSF を CSV（または 32bit float raw）で保存 |
| FR-20 | MTF 表示 | C | 2D MTF と、水平/垂直/放射平均の 1D MTF。理想 MTF を重畳 |
| FR-21 | スルーフォーカス | C | デフォーカス項を範囲指定で掃引し、アニメーション表示および XZ 断面表示 |
| FR-22 | 比較表示 | C | 現在の状態をスナップショットとして保持し、A/B 並置または差分表示 |
| FR-23 | CLI モード | S | `ZernikePsf.Cli --in coeffs.json --out psf.png` 形式でバッチ実行（回帰テストにも使用） |
| FR-24 | 多言語 UI | S | 日本語 / 英語。既定は OS ロケールに従う |

---

## 7. データ仕様

### 7.1 設定ファイル（JSON）

拡張子 `.zpsf.json`。UTF-8 (BOM なし)。

```json
{
  "schemaVersion": 1,
  "wavelengthNm": 550.0,
  "coefficientUnit": "waves",
  "indexScheme": "OSA",
  "normalized": true,
  "coefficients": [
    { "n": 2, "m": 0,  "value": 0.05 },
    { "n": 4, "m": 0,  "value": 0.10 },
    { "n": 3, "m": 1,  "value": -0.02 }
  ],
  "pupil": {
    "shape": "circular",
    "pupilSamples": 256,
    "obscurationRatio": 0.0,
    "spider": { "count": 0, "widthSamples": 2.0, "rotationDeg": 0.0 },
    "apodization": { "type": "none", "width": 1.0 },
    "maskImagePath": null
  },
  "psf": {
    "samplingQ": 4.0,
    "fftSize": 1024,
    "normalization": "idealPeak"
  },
  "display": {
    "psfScale": "log",
    "psfLogFloorDb": -50.0,
    "psfZoomLambdaOverD": 16.0,
    "wavefrontColormap": "coolwarm",
    "psfColormap": "viridis",
    "excludePistonTilt": true
  }
}
```

- **係数は必ず `(n, m)` で保存する。** インデックス番号で保存すると方式変更時に破綻するため。
- `schemaVersion` が未知の場合は読み込みを中止し、理由をダイアログで表示する。
- 未知キーは無視する（前方互換）。欠落キーは既定値で補う。

### 7.2 係数 CSV

読み込み時は次の 2 形式を自動判別する。ヘッダ行（`#` 始まりまたは非数値行）は無視。

```
# 形式 1: インデックス, 値   （インデックス方式は UI の現在設定に従う）
4,0.05
11,0.10

# 形式 2: n, m, 値           （方式に依存しないため推奨）
2,0,0.05
4,0,0.10
```

区切りはカンマ / タブ / 空白を許容。小数点はピリオドのみ（ロケール非依存で
`InvariantCulture` により解釈）。書き出しは常に形式 2。

### 7.3 マップ CSV

`N` 行 × `N` 列の数値。1 行目に `# key=value` 形式のメタ情報を出力する。

```
# type=wavefront unit=waves size=256 pupilSamples=256
# type=psf unit=normalized size=1024 samplingQ=4 lambdaOverD_perPixel=0.25
```

---

## 8. 非機能要件

### 8.1 動作環境

| 項目 | 要件 |
| --- | --- |
| OS | Windows 10 21H2 以降 / Windows 11、x64 |
| ランタイム | 自己完結ビルドではランタイム不要。framework-dependent 版は .NET 8 Desktop Runtime |
| 権限 | 管理者権限不要。ユーザ書込可能な任意フォルダから実行可 |
| 画面 | 1366×768 以上。高 DPI (150% / 200%) 対応（`PerMonitorV2`） |
| メモリ | `N = 2048` 時のピークで 512 MB 以内 |

### 8.2 性能

| 条件 | 目標 |
| --- | --- |
| 係数 1 個変更 → 再表示 (`N=1024`, `D_pix=256`) | **100 ms 以内** |
| スライダドラッグ中 | プレビュー品質（`N=512`）で **30 fps 以上**、離した時点でフル品質に再計算 |
| 起動から入力可能まで | 2 秒以内 |
| `N=2048` でのフル計算 | 1 秒以内 |

計算は UI スレッドから分離し（`Task` + `CancellationToken`）、新しい入力が来たら
実行中の計算をキャンセルして最新値で再実行する（**最新値のみを処理し、中間の入力は捨てる**）。

### 8.3 配布

- 成果物: `ZernikePsfViewer.exe` 単一ファイル（自己完結・トリム有効）
- ZIP を展開してそのまま実行できること（レジストリを汚さない）
- 設定は実行ファイルと同じフォルダの `settings.json`、書込不可なら
  `%LOCALAPPDATA%\ZernikePsfViewer\` にフォールバック
- 未署名 exe は SmartScreen / AV に警告される可能性がある。README に回避手順を記載し、
  可能であればコード署名を行う

### 8.4 その他

| 項目 | 要件 |
| --- | --- |
| ロギング | 例外発生時に `logs/` へスタックトレースを出力。UI には要約のみ表示 |
| 入力検証 | 数値欄は範囲外・非数値を拒否し、直前の有効値に戻す。異常終了させない |
| 操作性 | 主要操作にキーボードショートカット（付録 B） |
| 保守性 | `ZernikePsf.Core` は GUI 参照を持たないこと（TUI / CLI / PowerShell から再利用するため） |

---

## 9. UI 仕様（主案 A: WinForms）

### 9.1 画面レイアウト

```
┌──────────────────────────────────────────────────────────────────────────┐
│ ファイル(F)  編集(E)  表示(V)  ツール(T)  ヘルプ(H)                       │
├───────────────────────┬──────────────────────────────────────────────────┤
│ ■ 光学設定             │  ┌── 瞳面（波面）──────┐ ┌── PSF ─────────────┐ │
│  λ [nm]      [550   ] │  │                      │ │                    │ │
│  瞳サンプル  [256   ] │  │    位相マップ         │ │   強度マップ        │ │
│  サンプリングq [4.0 ] │  │    + カラーバー       │ │   + カラーバー      │ │
│  → FFT N = 1024       │  │                      │ │                    │ │
│  遮蔽比 ε    [0.00  ] │  └──────────────────────┘ └────────────────────┘ │
│  スパイダー  [なし ▼] │  ┌── 断面プロット ─────────────────────────────┐ │
│                       │  │ ── 実測  ┄┄ 理想 Airy                       │ │
│ ■ Zernike 係数         │  └─────────────────────────────────────────────┘ │
│  方式 [OSA ▼]         │  ┌── 指標 ─────────────────────────────────────┐ │
│  単位 [λ ▼] 正規化[✓] │  │ RMS 0.083 λ  PV 0.41 λ  Strehl 0.82         │ │
│  n_max [4 ▼]          │  │ Maréchal 0.79  FWHM 1.08 λ/D  EE(3λ/D) 0.71 │ │
│ ┌───┬────┬──────┬───┐ │  │ ☑ ピストン/チルトを除外                      │ │
│ │ j │ 名称│ 値   │◀▶ │ │  └─────────────────────────────────────────────┘ │
│ │ 0 │ピスト│ 0.00 │──○│ │  表示: PSF [対数 ▼] 下限[-50 dB] ズーム[±16 λ/D]│
│ │ 4 │デフォ│ 0.05 │─○─│ │                                                  │
│ │12 │球面 │ 0.10 │──○│ │                                                  │
│ └───┴────┴──────┴───┘ │                                                  │
│  [クリア][ランダム]    │                                                  │
├───────────────────────┴──────────────────────────────────────────────────┤
│ 準備完了 │ N=1024 q=4.0 │ 計算 42 ms                                       │
└──────────────────────────────────────────────────────────────────────────┘
```

- 左ペイン（固定幅 380 px、スクロール可）: 入力。右ペイン: 表示。`SplitContainer` で可変。
- 画像ペインは正方形を維持し、ウィンドウリサイズに追従する。
- ステータスバーに最終計算時間を出す（性能劣化に気付けるようにする）。

### 9.2 表示仕様

| 対象 | 既定カラーマップ | スケール |
| --- | --- | --- |
| 波面（位相） | 発散型 coolwarm（0 = 白）。**0 を中心に対称なレンジ**にする | リニア。レンジは自動 or 手動 |
| 瞳振幅 | グレースケール | 0〜1 固定 |
| PSF | viridis（他に grayscale, inferno, hot） | 対数（既定, 下限 −50 dB）/ リニア / γ (0.1〜1.0) |
| MTF | viridis | リニア 0〜1 |

- マスク外領域は中間グレー（`#404040`）で塗り、データ無しを明示する。
- カラーバーは画像の右に常時表示し、最小・最大値を数値で添える。
- マウスカーソル位置の座標と値をステータスバーに表示する
  （瞳面: `(x/R, y/R), W [λ]` / PSF: `(λ/D 単位座標), 正規化強度`）。

### 9.3 対話

| 操作 | 動作 |
| --- | --- |
| スライダのドラッグ | 該当係数を連続変更、プレビュー品質で追従 |
| 表セルへの数値入力 + Enter | 確定して再計算 |
| 画像上でホイール | ズーム（PSF は `λ/D` 範囲、瞳面は等倍固定） |
| 画像上で右クリック | コンテキストメニュー（PNG 保存 / CSV 保存 / レンジ自動調整） |
| Ctrl+Z / Ctrl+Y | 係数変更のみ Undo / Redo（32 段） |

---

## 10. テスト計画

### 10.1 単体テスト（`ZernikePsf.Tests`）

| ID | 対象 | 期待値 |
| --- | --- | --- |
| UT-01 | `R_n^m` の値 | `R_4^0(0) = 1`, `R_4^0(1) = 1`, `R_4^0(0.5) = -0.125`, `R_2^0(ρ)=2ρ²−1` を全域で一致（誤差 < 1e-12） |
| UT-02 | 正規化 Zernike の直交性 | `D_pix = 512` で `n ≤ 6` の全ペアについて `(1/π)∫Z_iZ_j ≈ δ_ij`（誤差 < 5e-3） |
| UT-03 | インデックス変換の全単射 | OSA・Noll・Fringe それぞれで `n ≤ 10` の範囲を往復変換して同一 `(n,m)` に戻ること |
| UT-04 | インデックス既知値 | 付録 A の表と完全一致 |
| UT-05 | FFT | ランダム配列で naive DFT と一致（N=64, 誤差 < 1e-10）。逆変換の往復同一性 |
| UT-06 | パーセバル則 | `Σ\|P\|² = (1/N²)Σ\|FFT(P)\|²`（誤差 < 1e-10） |
| UT-07 | 無収差 PSF | 円形瞳・全係数 0 のとき、第 1 暗環半径が `1.22 λ/D` と 1% 以内で一致、Strehl = 1.000 |
| UT-08 | Airy 一致 | 無収差 PSF の放射断面が解析式 `[2J₁(v)/v]²` と相対誤差 2% 以内（ピーク近傍 5 λ/D まで） |
| UT-09 | チルトの効果 | RMS 正規化 `Z_1^1` に係数 `c` を与えると PSF が `4c · λ/D` だけ移動する（誤差 0.1 画素以内、`q=8`） |
| UT-10 | Maréchal 近似 | 純デフォーカス `σ = 0.02〜0.08 λ` で厳密 Strehl と `exp(−(2πσ)²)` が 2% 以内で一致 |
| UT-11 | 遮蔽瞳 | `ε = 0.5` のとき無収差 PSF のピークが下がり第 1 暗環が内側に寄る。総エネルギー保存を確認 |
| UT-12 | MTF | 無収差・円形瞳の MTF がカットオフ `f_c` で 0 に達し、解析式 `(2/π)(arccos ν − ν√(1−ν²))` と 1% 以内で一致 |
| UT-13 | JSON 往復 | 保存 → 読込で全パラメータが一致 |
| UT-14 | CSV 解析 | 形式 1/2、区切り違い、BOM 付き、CRLF/LF、空行・コメント行の混在を正しく処理 |

### 10.2 結合・受入テスト

| ID | 内容 | 合格条件 |
| --- | --- | --- |
| IT-01 | 8.2 節の性能目標 | クリーンな Windows 11 / 中位ノート PC で全項目を満たす |
| IT-02 | UC-1〜UC-5 の通し操作 | 例外を出さずに完了し、期待どおりの出力が得られる |
| IT-03 | 高 DPI (200%) 表示 | レイアウト崩れ・文字切れがない |
| IT-04 | 長時間操作 | スライダを 10 分連続操作してもメモリ増加が 50 MB 以内（`Bitmap` のリーク検出） |
| IT-05 | 異常系 | 存在しないファイル、壊れた JSON、`ε = 0.99`、`q = 1`、`n_max = 20` で落ちない |
| IT-06 | 配布物 | ZIP 展開のみで起動。`Program Files` 以外の任意フォルダ、非管理者アカウントで動作 |

### 10.3 参照値の作り方

開発機（Windows 以外・Python 利用可の環境で構わない）で numpy / poppy による
リファレンス出力を生成し、`tests/fixtures/*.csv` として **数値だけをリポジトリに固定する**。
これにより、製品側・テスト実行側はいずれも Python に依存しない。

---

## 11. 開発計画

| マイルストーン | 内容 | 目安 |
| --- | --- | --- |
| M1 | `ZernikePsf.Core` + CLI。UT-01〜UT-12 が緑になること | 実装の山場。ここで数値を確定させる |
| M2 | WinForms 最小 GUI（係数表・波面・PSF・リアルタイム更新） | FR-01〜FR-14 |
| M3 | 指標・入出力・エクスポート | FR-15〜FR-19, FR-23 |
| M4 | 仕上げ（高 DPI・多言語・性能調整・配布パッケージ） | IT-01〜IT-06 |
| M5 (任意) | MTF・スルーフォーカス・比較表示・TUI | FR-20〜FR-22 |

M1 を先に単体で完成させ、数値の正しさを GUI と切り離して確定させることを強く推奨する。

---

## 12. リスクと対策

| リスク | 影響 | 対策 |
| --- | --- | --- |
| FFT のスケール・シフト規約の取り違え | PSF が微妙にずれ、気付きにくい | UT-07〜UT-09（Airy 半径・チルト移動量）で規約ごと検証する |
| インデックス方式の混同 | 別の収差を表示してしまう | 内部を `(n,m)` に統一。UI に常時 `(n,m)` を併記。UT-03/UT-04 |
| 遮蔽瞳での係数由来 RMS の誤用 | 指標が実態とずれる | RMS は必ずマップから数値計算。UI に注記 |
| 単一 exe のサイズ・AV 誤検知 | 配布時の摩擦 | トリム有効化、framework-dependent 版も併せて配布、可能なら署名 |
| GUI スレッドでの重い計算 | フリーズ | 計算は別スレッド。キャンセル可能。プレビュー品質の二段構え |
| 高次項での階乗オーバーフロー | 誤った多項式 | 整数テーブル + `n ≤ 20` の上限チェック |
| 対象 PC に何も置けない事態 | 配布不能 | 案 E（PowerShell 単一ファイル）を代替経路として維持 |

---

## 13. 参考文献

1. R. J. Noll, "Zernike polynomials and atmospheric turbulence," *JOSA* **66**(3), 207–211 (1976).
2. J. W. Goodman, *Introduction to Fourier Optics*, 4th ed., W. H. Freeman (2017). — 4〜6 章
3. M. Born and E. Wolf, *Principles of Optics*, 7th ed., Cambridge (1999). — 9 章（収差論）
4. V. N. Mahajan, *Optical Imaging and Aberrations, Part I / II*, SPIE Press.
5. ANSI Z80.28 — Methods for Reporting Optical Aberrations of Eyes（OSA/ANSI インデックス）
6. ISO 24157 — Ophthalmic optics: Zernike の表記法

---

## 付録 A. Zernike インデックス対応表

正規化係数 `N` は RMS 正規化時の値。多項式は `N` を掛ける前の
`R_n^|m|(ρ) · cos/sin(mθ)` を示す。

| OSA/ANSI | Noll | Fringe | n | m | 名称 | 多項式 (正規化前) | N |
| ---: | ---: | ---: | ---: | ---: | --- | --- | --- |
| 0 | 1 | 1 | 0 | 0 | ピストン | 1 | 1 |
| 1 | 3 | 3 | 1 | −1 | Y チルト | ρ sinθ | 2 |
| 2 | 2 | 2 | 1 | +1 | X チルト | ρ cosθ | 2 |
| 3 | 5 | 6 | 2 | −2 | 非点収差 (±45°) | ρ² sin2θ | √6 |
| 4 | 4 | 4 | 2 | 0 | デフォーカス | 2ρ²−1 | √3 |
| 5 | 6 | 5 | 2 | +2 | 非点収差 (0/90°) | ρ² cos2θ | √6 |
| 6 | 9 | 11 | 3 | −3 | トレフォイル (Y) | ρ³ sin3θ | √8 |
| 7 | 7 | 8 | 3 | −1 | コマ収差 (Y) | (3ρ³−2ρ) sinθ | √8 |
| 8 | 8 | 7 | 3 | +1 | コマ収差 (X) | (3ρ³−2ρ) cosθ | √8 |
| 9 | 10 | 10 | 3 | +3 | トレフォイル (X) | ρ³ cos3θ | √8 |
| 10 | 15 | 18 | 4 | −4 | クアドラフォイル (Y) | ρ⁴ sin4θ | √10 |
| 11 | 13 | 13 | 4 | −2 | 二次非点収差 (±45°) | (4ρ⁴−3ρ²) sin2θ | √10 |
| 12 | 11 | 9 | 4 | 0 | 球面収差 | 6ρ⁴−6ρ²+1 | √5 |
| 13 | 12 | 12 | 4 | +2 | 二次非点収差 (0/90°) | (4ρ⁴−3ρ²) cos2θ | √10 |
| 14 | 14 | 17 | 4 | +4 | クアドラフォイル (X) | ρ⁴ cos4θ | √10 |
| 15 | 21 | 27 | 5 | −5 | ペンタフォイル (Y) | ρ⁵ sin5θ | √12 |
| 16 | 19 | 20 | 5 | −3 | 二次トレフォイル (Y) | (5ρ⁵−4ρ³) sin3θ | √12 |
| 17 | 17 | 15 | 5 | −1 | 二次コマ収差 (Y) | (10ρ⁵−12ρ³+3ρ) sinθ | √12 |
| 18 | 16 | 14 | 5 | +1 | 二次コマ収差 (X) | (10ρ⁵−12ρ³+3ρ) cosθ | √12 |
| 19 | 18 | 19 | 5 | +3 | 二次トレフォイル (X) | (5ρ⁵−4ρ³) cos3θ | √12 |
| 20 | 20 | 26 | 5 | +5 | ペンタフォイル (X) | ρ⁵ cos5θ | √12 |
| 24 | 22 | 16 | 6 | 0 | 二次球面収差 | 20ρ⁶−30ρ⁴+12ρ²−1 | √7 |

> 「X / Y」「0/90°・±45°」の呼称は文献により逆になることがある。**本ツールは
> `(n, m)` を正とし、名称は表示用ラベルにすぎない**旨を UI のツールチップに明記する。

---

## 付録 B. キーボードショートカット

| キー | 動作 |
| --- | --- |
| Ctrl+N | 全係数をクリア（新規） |
| Ctrl+O / Ctrl+S | 設定の読込 / 保存 |
| Ctrl+Shift+S | 名前を付けて保存 |
| Ctrl+E | 表示中の画像を PNG エクスポート |
| Ctrl+Z / Ctrl+Y | 係数変更の Undo / Redo |
| ↑ / ↓ | 選択セルの係数を ±0.01 λ |
| Shift+↑ / ↓ | ±0.001 λ（微調整） |
| PageUp / PageDown | ±0.1 λ |
| L | PSF スケールをリニア ⇔ 対数 |
| R | 表示レンジを自動調整 |
| Space | 無収差状態と現在状態を一時的に切替表示（比較用） |
| F1 | ヘルプ |

---

## 付録 C. 代替案 E（PowerShell）の構成メモ

「対象 PC にビルド成果物すら置けない」場合の逃げ道。単一の `ZernikePsfViewer.ps1` で完結する。

```powershell
# 1) 数値カーネルを C# ソース文字列として埋め込み、初回起動時に JIT コンパイル
Add-Type -TypeDefinition @"
public static class Kernel {
    public static void Fft2(double[] re, double[] im, int n) { /* ... */ }
    public static double[] Wavefront(double[] coeff, int nPupil) { /* ... */ }
}
"@ -ReferencedAssemblies System.Drawing

# 2) GUI は System.Windows.Forms を直接叩く
Add-Type -AssemblyName System.Windows.Forms, System.Drawing
$form = New-Object System.Windows.Forms.Form
# ... DataGridView / TrackBar / PictureBox を配置
```

制約と注意:

- **PowerShell のループで FFT を回してはならない。** 1024² の 2D FFT は
  PowerShell スクリプトのままだと数十秒〜数分かかり、要件を満たさない。
  重い処理は必ず `Add-Type` 側（C#）に置く。
- Windows PowerShell 5.1 前提（OS 標準）。PowerShell 7 では `Add-Type` の
  `-ReferencedAssemblies` 指定が異なるため、両対応するなら分岐が必要。
- 実行ポリシーの回避が必要な場合がある:
  `powershell -ExecutionPolicy Bypass -File ZernikePsfViewer.ps1`
- 機能は FR-01, FR-05, FR-07（円形＋遮蔽のみ）, FR-08, FR-10, FR-13, FR-17 に絞る。
- 主案 A が使える場合は、同じ `ZernikePsf.Core.dll` を
  `Add-Type -Path .\ZernikePsf.Core.dll` で読み込む構成に切り替えられる。

---

## 付録 D. 代替案 G（TUI）の構成メモ

SSH / リモート・GUI 不可環境向け。`ZernikePsf.Core` を共有し、フロントのみ差し替える。

- 描画: Unicode 半角ブロック `▀` の前景色/背景色を別々に指定し、
  **1 セルで縦 2 画素**を表現する（24bit True Color、Windows Terminal / ConHost v2 対応）。
  非対応端末向けに ASCII 濃淡（` .:-=+*#%@`）へのフォールバックを用意する。
- Sixel 画像表示は Windows Terminal 1.22 以降でのみ利用可能。**必須要件にはしない。**
- レイアウト: 左に係数リスト（↑↓ で項選択、←→ で値増減）、右に瞳面と PSF、下に指標。
- 解像度は端末サイズに合わせてダウンサンプルする（表示用のみ。計算は本来の解像度）。
- ライブラリ: C# なら `Spectre.Console`、Rust なら `ratatui` + `crossterm`。

---

## 付録 E. 未決事項・確認したい点

| ID | 内容 | 暫定の既定 |
| --- | --- | --- |
| Q-1 | 主に使うインデックス方式はどれか（Noll か OSA か Fringe か） | OSA/ANSI を既定、3 方式とも対応 |
| Q-2 | 係数の単位は λ か nm か。RMS 正規化の有無 | λ・RMS 正規化を既定、切替可 |
| Q-3 | 中心遮蔽・スパイダーは必要か（望遠鏡系か、レンズ系か） | 実装するが既定は単純円形瞳 |
| Q-4 | 単色で十分か、多波長合成が要るか | 単色。多波長は v2 |
| Q-5 | 想定する最大次数 | n ≤ 10（OSA j ≤ 65）まで |
| Q-6 | 配布先 PC に .NET 8 ランタイムを入れられるか | 自己完結ビルドを既定にして依存を回避 |
| Q-7 | MTF・スルーフォーカスの優先度 | v1 では対象外（FR-20/21 は C） |
| Q-8 | UI 言語 | 日本語を既定、英語も用意 |

これらは実装に着手する前に確定させたい。特に **Q-1・Q-2 は数値の意味そのものが変わる**ため、
既存データとの突き合わせがある場合は早期に確認が必要。
